#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <strings.h>
#include <time.h>

const int InitialCount = 100;
int Count, Num, Motion, ch;
bool Correct;
void pravila();
int mi(int* a, int* b, bool* c);
int ci(int* a, int* b);
int cm(int* a);
int eg(int* a);
int egpvp(int* a);